# /ship

Run the final quality gate.

Verify:
- project starts
- imports work
- required files exist
- tests/smoke checks pass
- no secrets are committed
- documentation is current
- incomplete features are clearly marked

Then provide a concise release summary.
